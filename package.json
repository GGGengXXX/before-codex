{
  "name": "codex-relay",
  "version": "0.1.0",
  "private": true,
  "description": "A small OpenAI Responses-compatible relay with multi-key and multi-endpoint failover.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "start:background": "sh scripts/start-relay.sh",
    "dev": "node --watch src/index.js",
    "cli": "node src/cli.js",
    "relayctl": "node src/cli.js",
    "test": "node --test",
    "check": "node --check src/index.js && for file in src/*.js test/*.test.js; do node --check \"$file\"; done"
  }
}
